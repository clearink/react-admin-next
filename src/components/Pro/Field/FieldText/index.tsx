import { FieldTextProps } from "./interface";

export default function FieldText(props: FieldTextProps) {
    const { text } = props
    // console.log('field text props',props);
    return <div>field text</div>
}