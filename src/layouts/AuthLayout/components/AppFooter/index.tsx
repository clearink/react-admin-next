import { Layout } from "antd";
import styles from "./style.module.scss";
export default function AppFooter() {
  return (
    <Layout.Footer className={styles.app_footer}>REACT ADMIN © CLEARINK</Layout.Footer>
  );
}
